name: Deploy Notebooks to Databricks

on:
  push:
    branches: [ main ]
    paths:
      - 'databricks/notebooks/**'

jobs:
  deploy:
    name: Deploy to Workspace
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Databricks CLI
        uses: databricks/setup-cli@main

      - name: Deploy Notebooks
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          # Wipe the old folder (ignores error if it doesn't exist yet)
          databricks workspace delete /Shared/github_pipeline --recursive || true

          # Recreate the folder
          databricks workspace mkdirs /Shared/github_pipeline

          # Upload all notebooks fresh
          databricks workspace import-dir ./databricks/notebooks /Shared/github_pipeline --overwrite

          echo "✅ Notebooks deployed successfully to /Shared/github_pipeline"
