name: github_events
version: "1.0.0"
config-version: 2
profile: github_events

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  github_events:
    silver:
      +materialized: view          # thin staging view over the Silver Delta table
      +schema: silver
    gold:
      +materialized: table         # Delta tables Power BI reads
      +file_format: delta
      +schema: gold
